{
  "name": "arbitragex-supreme-edge",
  "version": "3.0.0",
  "description": "ArbitrageX Supreme V3.0 - Cloudflare Workers Edge Computing Backend",
  "main": "workers/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "preview": "wrangler preview",
    "tail": "wrangler tail",
    "types": "wrangler types",
    "lint": "eslint workers/**/*.ts",
    "format": "prettier --write workers/**/*.ts"
  },
  "dependencies": {
    "hono": "^4.0.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "4.20250705.0",
    "wrangler": "^3.78.0",
    "typescript": "^5.0.0",
    "@types/node": "^20.0.0"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  }
}