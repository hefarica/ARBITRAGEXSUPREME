{
  "name": "@arbitragex/blockchain-connectors",
  "version": "1.0.0",
  "description": "Conectores blockchain reales para ArbitrageX Pro 2025",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rimraf dist",
    "test": "npm run build && node dist/main.js test",
    "demo": "npm run build && node dist/main.js demo",
    "start": "npm run build && node dist/main.js start",
    "analyze": "npm run build && node dist/main.js analyze",
    "monitor": "npm run build && node dist/main.js monitor",
    "help": "npm run build && node dist/main.js help"
  },
  "dependencies": {
    "ethers": "^6.0.0",
    "@solana/web3.js": "^1.87.0", 
    "@cosmjs/stargate": "^0.32.0",
    "cardano-serialization-lib": "10.0.0-beta.16",
    "bitcoin-core": "^4.2.0",
    "axios": "^1.6.0",
    "ws": "^8.14.0",
    "near-api-js": "^2.1.0",
    "@openzeppelin/contracts": "^4.9.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "@types/ws": "^8.5.0"
  }
}